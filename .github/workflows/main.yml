name: Shiksha News Mirror Bot

on:
  workflow_dispatch:
    inputs:
      clean_previous_run:
        description: "Fresh test — wipe state and re-send all 10 feed posts (newest first)"
        required: false
        default: false
        type: boolean
  schedule:
    - cron: "3/5 * * * *"

concurrency:
  group: shikshanews-mirror
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  mirror-bot:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Diagnose feed and channels
        continue-on-error: true
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          DEST_CHANNEL: "@testsourcechannelA"
          FEED_URL: https://tg.i-c-a.su/rss/shikshanewsrajasthan
          WP_URL: https://positronacademy.in
          WP_USER: ${{ secrets.WP_USER }}
          WP_PASS: ${{ secrets.WP_PASS }}
        run: python diagnose.py

      - name: Clean previous bot state
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.clean_previous_run }}
        shell: bash
        run: |
          if [ -f bot_state.sqlite3 ]; then
            rm -f bot_state.sqlite3
            echo "Removed bot_state.sqlite3"
          fi

      - name: Run mirror bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          DEST_CHANNEL: "@testsourcechannelA"
          FEED_URL: https://tg.i-c-a.su/rss/shikshanewsrajasthan
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          WP_URL: https://positronacademy.in
          WP_USER: ${{ secrets.WP_USER }}
          WP_PASS: ${{ secrets.WP_PASS }}
          WP_REFERER: https://positronacademy.in/wp-admin/
          ADMIN_CHAT_ID: ${{ secrets.ADMIN_CHAT_ID }}
          SKIP_WORDPRESS: "false"
          MAX_RUN_SECONDS: ${{ github.event_name == 'workflow_dispatch' && inputs.clean_previous_run && '900' || '540' }}
          DB_FILE: bot_state.sqlite3
          MAX_ITEMS_PER_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.clean_previous_run && '10' || '3' }}
          ITEM_DELAY_SECONDS: "3"
          FLOOD_MAX_RETRIES: "8"
          MAX_RETRIES: "3"
          WP_POST_TYPE: pages
          WP_TIMEOUT: "45"
          WP_MAX_RETRIES: "3"
          WP_SKIP_EXISTING: "true"
          WP_UPLOAD_MEDIA: "false"
          MAX_SOURCE_PAGES_PER_ITEM: "3"
          FETCH_SOURCE_FOR_LINKS: "true"
          PAGE_BUILD_MODE: mirror
          SOURCE_PAGE_HOSTS: indianaukrihelp.com
          SKIP_MESSAGE_PHRASES: "शिक्षा विभाग समाचार"
          WEB_FOLLOW_LINE: ""
          BRAND_IMAGES: "false"
          IMAGE_BRAND_NAME: POSITRON ACADEMY
          IMAGE_BRAND_ADDRESS: "चौधरी हॉस्पिटल के पास, पांसल चौराहा,भीलवाड़ा"
          IMAGE_BRAND_ADDRESS_LATIN: "चौधरी हॉस्पिटल के पास, पांसल चौराहा,भीलवाड़ा"
          IMAGE_BRAND_CONTACT: "8104894648"
          FOLLOW_LINE_TG: ""
          FOLLOW_LINE_WA: ""
          FOLLOW_LINE: ""
          ACADEMY_WEBSITE: https://positronacademy.in
          TELEGRAM_HANDLE_REPLACE: "@KapilRJ06"
        run: python shiksha_mirror.py

      - name: Commit SQLite state
        if: always()
        shell: bash
        run: |
          if [ -f bot_state.sqlite3 ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add bot_state.sqlite3
            if git diff --cached --quiet; then
              echo "bot_state.sqlite3 unchanged"
            else
              git commit -m "Update bot sqlite state"
              git push origin HEAD:main || true
            fi
          fi
